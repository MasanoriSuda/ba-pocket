# BA Pocket (PoC)

## 実行方法（Android）
1. Flutter SDK をセットアップ
2. 依存取得

```bash
flutter pub get
```

3. 端末/エミュレータを確認

```bash
flutter devices
```

4. 実行（Android）

```bash
flutter run -d <device_id>
```

`<device_id>` は `flutter devices` で表示されるIDを指定してください。 
